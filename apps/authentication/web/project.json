{
  "name": "authentication-web",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/authentication/web/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/authentication/web"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nrwl/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "authentication-web:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "authentication-web:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "authentication-web:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nrwl/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "authentication-web:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "authentication-web:build:development"
        },
        "production": {
          "buildTarget": "authentication-web:build:production"
        }
      }
    },
    "test": {
      "executor": "@nrwl/vite:test",
      "outputs": ["coverage/apps/authentication"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../../coverage/apps/authentication"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/authentication/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
